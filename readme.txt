extract all files into a directory. the test image folders (Apple\ and \Elephant\ ) must be on the same directory.

to run on image set Apple, run Apple.m
to run on image set Elephant, run Elephant.m

The result images will be "normal_map.png", "albedo.png", "re-rendered.png"